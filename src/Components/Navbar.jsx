import React from 'react'
import PropTypes from 'prop-types'

const Navbar = ({carts}) => {
  return (
    <div>
      
    </div>
  )
}

Navbar.propTypes = {
carts: PropTypes.array
}

export default Navbar
